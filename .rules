# Rust coding guidelines

- Use `./script/clippy` instead of `cargo clippy`
- Use `anyhow` for error handling in application code; prefer `?` over `unwrap()`
- Avoid `unwrap()` across FFI boundaries — a panic across JNI/C is undefined behaviour

---

# Skills

Detailed quick-reference documentation is in `.skills/`. Load the relevant file(s) before working on any of the topics below.

## GPUI framework — `.skills/gpui.md`

Context types (`App`, `Context<T>`, `Window`, `AsyncApp`), entities (`Entity<T>`, `WeakEntity<T>`), rendering (`Render`, `RenderOnce`), styling and layout, input events, actions, focus management, entity events and subscriptions, concurrency (`cx.spawn`, `cx.background_spawn`, `Task<R>`), global state, custom elements, notify, per-frame keyed state (`use_keyed_state`), app setup, error handling, recent API changes (never use `Model<T>`, `View<T>`, `WindowContext`, `ViewContext<T>`, `ModelContext<T>`), Rust 2024 borrow checker gotchas.

## gpui-component library — `.skills/gpui-component.md`

Setup and `Root` overlay layers, required trait imports, layout helpers (`h_flex`, `v_flex`), theming (`ActiveTheme`, `cx.theme()`), and all components: Button, Tag, Toggle, Switch, Checkbox, Input, Slider, Select, Form/Field, Tabs, List, Dialog, Notification (toast), Popover, Divider, Label, Progress, Spinner, Tooltip, Icon, Sidebar, Command Palette. Common patterns: entity creation, subscriptions, conditional rendering, `AnyElement`, Rust 2024 gotchas, collapsible sections, loading data from stores.

## gpui_transitions crate — `.skills/gpui_transitions.md`

Animated value transitions. Setup (including required `[patch.crates-io]` in workspace `Cargo.toml`), `Transition<T>`, `Lerp` trait, `BoolLerp<N>`, `WindowUseTransition` extension (`use_keyed_transition`, `use_transition`), evaluating, updating goals, easing functions, continuous mode, `use_keyed_state` (non-animated sibling).

## Android + Rust JNI — `.skills/android.md`

HTTP store pattern (Retrofit + OkHttp + kotlinx.serialization), network security config for cleartext, `JsonElement` approach for Rust external-tagged enums, extension property import gotcha, `RustBridge` stub → native progression, `StateFlow<UiState>`, `collectAsStateWithLifecycle`, Gradle/KGP 2.x config gotchas, serde gotchas (`chrono::Duration`, `uuid`).

## cargo-ndk + Rust JNI Bridge — `.skills/cargo-ndk-bridge.md`

Building a Rust `cdylib` for Android: toolchain setup (`cargo-ndk`, `rustup target add`), workspace membership requirement, JNI naming with underscore-escaping (`_` → `_1` in package names), impl/wrapper split pattern, build commands (dev arm64 vs release all ABIs), Kotlin stub → native progression, HTTP+bridge vs SQLite+bridge architectures.

## Android Compose Material 3 Expressive — `.skills/compose.md`

Dependencies (use `compose-bom-alpha` for Expressive), Activity setup, Navigation 3, simple `NavigationBar` tab pattern, Scaffold + FAB, `LargeTopAppBar` with scroll collapse, Buttons with `shapes`, `SplitButtonLayout`, `ButtonGroup`, `FloatingActionButtonMenu`, `HorizontalFloatingToolbar`, `FlexibleBottomAppBar`, `WideNavigationRail`, wavy/loading progress indicators (`LoadingIndicator`, `PullToRefreshBox`), `TopAppBar`, `ModalBottomSheet` with `imePadding` + auto-focus, `LazyColumn`, `ListItem` (all slots + `ListItemDefaults.colors`), mixed-type sealed-class lists, surface container color scale, theming, accessibility, Gradle notes.

## Wear OS standalone app — `.skills/wear-os.md`

Standalone Wear OS app with Kotlin/Compose (no companion dependency required). Dependencies and `build.gradle.kts` (Wear Compose versioned independently — no BOM), `AppScaffold` + `ScreenScaffold`, `TransformingLazyColumn` (three required pieces: `transformedHeight`, `SurfaceTransformation`, `rememberTransformationSpec`), non-clickable `Card` (1.6+), scroll performance rules, architecture pattern (`Refreshing` state, optimistic mutations), phone ↔ watch communication via `MessageClient` (foreground + background listener wiring, `WearableListenerService`), deployment (`adb -t <transport-id>`), Tiles, Complications.

## Wear Compose Material 3 components — `.skills/wear-compose.md`

Screen structure, `TransformingLazyColumn` details, navigation (`SwipeDismissableNavHost`, Navigation3), buttons with shape morphing, `EdgeButton`, `ButtonGroup`, toggle controls, dialogs, pickers, progress indicators, cards, pagers, `SwipeToReveal`, sliders/steppers, ripple, theming, ambient mode, rotary input, `CurvedText`/`TimeText`, placeholders, hierarchical focus, accessibility.

---

# Project-specific context

## Crate architecture

The project is split into two dependency layers with a strict direction:

```
desktop / android / wear_os  (UI clients)
        ↓
   simple-core  ←  simple-db
```

- `simple-core` owns all domain types (`Action`, `ActionCompletion`, `Pipeline`, `QueueItem`, `Routine`, `Event`, etc.).
- `simple-db` owns all persistence. Its functions take a `&rusqlite::Connection` (not the `Arc<Mutex<…>>` wrapper) and return `simple_db::Result<T>`. Import functions directly:

```rust
use simple_db::{
    connect_and_migrate,        // → DatabaseConnection (Arc<Mutex<Connection>>)
    connect_and_migrate_at,     // same but accepts an explicit path (use this on Android/Wear)
    upsert_action, fetch_actions, fetch_action_by_id, delete_action,
    upsert_event, fetch_events, fetch_event_by_id, delete_event,
    upsert_routine, fetch_routines, fetch_routine_by_id, delete_routine,
    save_pipeline, load_pipeline, refresh_pipeline,
    insert_action_completion, delete_action_completion,
    fetch_completions_by_lineage, fetch_all_completions,
};
```

- `DatabaseConnection` is `Arc<Mutex<rusqlite::Connection>>`. Unlock it with `.lock().unwrap()` before passing `&conn` to `simple_db` functions.
- `Uuid` is from the `uuid` crate — import `uuid::Uuid` directly, never via `simple_core`.
- `chrono::Duration` with `chrono = { features = ["serde"] }`: `Option<Duration>` where `None` serializes as `null` (clean). `Some(d)` serializes as `{"secs": i64, "nanos": u32}`. On the Kotlin side, use `JsonElement?` to remain format-agnostic. Only needed for direct JNI bridges that avoid HTTP — see `.skills/cargo-ndk-bridge.md`.

## Design documents

- Product philosophy, core concepts, features, user needs, and implementation roadmap: **`DESIGN.md`** (workspace root)
- Desktop crate architecture and overlay system: **`crates/desktop/DESIGN.md`**
- Android architecture and conventions: **`android/DESIGN.md`**
